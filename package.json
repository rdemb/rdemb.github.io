{
  "name": "dlogic-studio-site",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bash -c 'ulimit -v unlimited 2>/dev/null; astro dev --host'",
    "build": "bash -c 'ulimit -v unlimited 2>/dev/null; astro build'",
    "preview": "bash -c 'ulimit -v unlimited 2>/dev/null; astro preview --host'"
  },
  "dependencies": {
    "astro": "^5.5.0",
    "@fontsource-variable/space-grotesk": "^5.1.0",
    "@fontsource-variable/inter": "^5.1.0",
    "@fontsource-variable/jetbrains-mono": "^5.1.0"
  }
}
